<UserControl x:Class="DynamicViewExample.Views.SignUp"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DynamicViewExample.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Grid.Column="1" Margin="20" VerticalAlignment="Top">
        <TextBlock Text="Create Operator" FontSize="20" FontWeight="Bold" Foreground="#2D572C" Margin="0,0,0,20" />
        <TextBlock Text="Username" Margin="0,0,0,5" />
        <TextBox x:Name="tb1" Height="30" Margin="0,0,0,15" />
        <TextBlock Text="Password" Margin="0,0,0,5" />
        <TextBox x:Name="tb2" Height="30" Margin="0,0,0,15" />
        <Button Content="Create User" Height="35" Background="#2D572C" Foreground="White" Click="OnCreateAccount_Click" Cursor="Hand" />

        <Button Content="Go to sign-in"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Margin="10"
                Click="GoToSignIn_Click"
                Background="Transparent"
                FontStyle="Italic"
                FontSize="12px"
                BorderThickness="0"
                Cursor="Hand"/>
    </StackPanel>
</UserControl>
